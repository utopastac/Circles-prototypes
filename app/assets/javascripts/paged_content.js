$(function(){ 
  // var pageHolder = $(".paged-content");
  // var pages = $("section", pageHolder);
  // var pagelinks = $("[data-page-target]", questionHolder);
  // var current = 0;
  // var history = []
  // pagelinks.click(nextQuestion);  
  // if(questions.length > 0) setUpQuestions();

  // function setUpPages(){
  //   //TweenMax.set(questionHolder, {minHeight: $(window).height() - navHeight + 70});
  //   TweenMax.set(pages, {autoAlpha: 0, x: 200, display: "none"});
  //   TweenMax.set(pages.eq(0), {autoAlpha:1, x: 0, display: "block"});
  // }

  // function showPage(target){
  //   TweenMax.to(pages.eq(current), 0.4, {autoAlpha:0, x: -200, display: "none", ease:Power2.easeOut});
  //   TweenMax.to(pages.eq(target), 0.4, {autoAlpha:1, x: 0, display: "block", ease:Power2.easeOut});
  //   current = target;
  // }

  // function goToPage(event){
  //   var elem = $(event.currentTarget);
  //   var pageTarget = $(elem.data("page-target"));
  //   var target = pages.index(pageTarget);
  //   showPage(target);
  //   return false;
  // }

  // function back(event){

  // }

});
